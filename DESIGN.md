# DESIGN.md — equal-earth-viewer

採用した設計判断と、その理由・代替案。

## 成果物を単一 HTML ファイルにする

**判断**: `vite-plugin-singlefile` で `docs/index.html` 1 ファイルに全部 inline する。

**Why**: これは教材であり、第一の性質は「配布物」であること。単一ファイルなら
ダブルクリックで開き、USB でもメールでも渡せ、静的ホスティングにもそのまま置ける。
ビルド環境も CDN 到達性も要求しないので、数年後に開いても動く可能性が高い。

**代替案**: 通常の Vite ビルド (JS/CSS 分割)。開発は同じだが配布のたびに
ディレクトリ一式を扱う必要があり、`file://` で開けない。教材としては損。

**Tradeoff**: 94 KB (gzip 39 KB) を 1 ファイルで読む。この規模では問題にならない。

## ソースは TypeScript strict + モジュール分割のまま

単一ファイル化は**出力側だけの判断**で、入力側は分割を保つ。1 ファイルの巨大 HTML を
直接編集する形にすると、型検査も差分レビューも失われる。

## 海岸線データは `land-110m.json` を使う

**判断**: `countries-110m.json` + `topojson.merge` ではなく、`land-110m.json` を `topojson.feature` する。

**Why**: world-atlas には陸塊そのものが同梱されている。国境線が不要なこのアプリでは
結果が同一で、ファイルは 105 KB → 54 KB と約半分。単一ファイル配布ではサイズが直接効く。
マージ処理も起動時から消える。

**将来**: 国境線を描く要件が出たら `countries-110m.json` + `merge` に戻す。

## 図法ごとに拡大率を 1 度だけ確定してキャッシュする

**判断**: `fitWidth` は図法の初回のみ実行し、以降は `rotate` だけを差し替える。
高さもそのとき確定して固定する。

**Why**: 擬円筒・円筒図法では中央経線を変えても投影された球の外郭は合同なので、
毎フレーム fit する必要がない。スライダー操作中の仕事が「rotate 変更 + `d` の引き直し」
だけになり、地図の縦位置が微動しない。

**この前提は実測で守る**: `scripts/verify.mjs` の項目 A が全 5 図法について
経度 5° 刻みで外郭 bounds の変動を測り、実測 0.0000 px であることを確認している。
斜軸投影を入れると前提が崩れるため、そのときはテストが落ちる。

## `index.html` はルート直下に置く

仕様書は `public/index.html` としていたが、Vite は `public/` を**変換せずコピーする**
ディレクトリとして扱うため、そこに置くとエントリとして処理されない。Vite の規約に従う。

## 地図の配色は CSS ではなく TypeScript に持つ

**判断**: `theme.ts` を地図色の正本とし、描画時に SVG 内の `<style>` へ焼き込む。
UI 側の色は `style.css` の CSS 変数が持つ (別系統)。

**Why**: SVG / PNG に書き出すと外部 CSS は効かない。CSS 変数を正本にすると
「画面では色が付くが書き出すと真っ黒」という食い違いが生まれる。色を TS 側に置き、
画面描画も書き出しも同じ `renderInto()` を通すことで、食い違いを構造的に排除した。

**書き出しは常に light テーマ**: スライドや配布資料に貼る用途では、
閲覧者のダークモード設定が成果物の背景色を決めてしまうのは望ましくない。

## URL には図法と表示トグルも含める

仕様は `?lon=` のみだったが、比較教材としては「この図法でティソーを表示した状態」を
そのまま共有できる価値が大きい。既定値と一致する項目は書き出さないので、
`?lon=150` のような短い URL は保たれる。

## 回転アニメーション中は URL を更新しない

`history.replaceState` を毎フレーム呼ぶのは無駄なので、`setState` に `commit` 引数を設け、
回転中は描画のみ行う。停止時に一度だけ現在位置を URL へ確定する。

## `d3-geo-projection` の型は自前で宣言する

同パッケージは型定義を同梱しておらず、`@types/d3-geo-projection` も存在しない (2026-09 時点で確認)。
使用する 4 図法ぶんだけを `src/types/externals.d.ts` に最小宣言した。
`any` で握り潰すより、使う関数の形だけを明示するほうが strict の利益を保てる。

## 検証は DOM を使わず投影計算で行う

**判断**: ブラウザ自動化ではなく、Node で d3-geo を直接叩く `scripts/verify.mjs` を置く。

**Why**: このアプリの正しさの核は「どの経度が地図のどこに出るか」であって、
DOM の組み立てではない。投影計算だけなら headless ブラウザなしで検証でき、
実行が速く壊れにくい。受け入れ条件 (150°E でグリーンランドが左右に分断される /
`?lon=-90` でアメリカ中心) をそのままテストにしている。

**限界**: レイアウト・配色・操作感は検証できない。そこは人間の目視が必要。

## 既定の中央経線はアメリカ中心 90°W

**判断**: `DEFAULT_STATE.lon = -90`。プリセットにも「アメリカ中心 90°W」を先頭で置く。

**Why**: このビューアの出発点は「アメリカ中心の地図も作ってあげたら納得するんじゃない?
ユーラシアが切れちゃうけど」という皮肉 (2026-09-05)。初期表示でユーラシアが裂けている
のを見せるのが教材としての狙いそのものなので、既定にした。

**なぜ 90°W か**: 合衆国本土の地理的中心は 99°W 付近だが、Patterson が公開している
Equal Earth 壁地図 3 版 (Africa/Europe = 10°E、Americas = 90°W、East Asia/Australia = 150°E)
に合わせた。既存プリセット 10°E / 150°E も同じ系列なので、3 版がそのまま揃う。
`scripts/verify.mjs` 項目 C も以前から `lon=-90` を「アメリカ中心」の受け入れ条件にしている。

## 地図の横ドラッグ / スワイプで中央経線を動かす

**判断**: `#map` に Pointer Events を張り、水平移動量を「表示幅 = 360°」で経度に換算する。
縦方向は `touch-action: pan-y` でブラウザのスクロールに譲る。

**Why**: スマホではスライダーのつまみが小さく、地図そのものを指で回す方が自然。
Pointer Events ならマウス・タッチ・ペンを 1 系統で扱える。換算比を表示幅基準にすると
指の下の経線がほぼ指に付いてくるので、操作感が予測できる。

**回転との排他**: ドラッグ開始で `stopSpin()`。ドラッグ中は `commit=false` で描画のみ、
指を離した時に整数に丸めて URL へ確定する (回転アニメーションと同じ規律)。
`pointercancel` (縦スクロールにブラウザが奪った場合) と `lostpointercapture` も終端として扱う。

## 地図を操作パネルより上に置く

**判断**: 見出し → 地図 → 中央経線パネル → 表示設定パネル の順。

**Why**: 主役は図であり、操作系は従。特にスマホでは操作パネルが先頭にあると
初期表示で地図がファーストビューに入らない。ドラッグ / スワイプで回せるようになったので、
地図が先頭でも操作は成立する (user 指摘、2026-09-05)。

## プリセットは Patterson の 3 版に揃える (0° は置かない)

**判断**: 90°W / 10°E / 150°E の 3 つだけ。「大西洋中心 0°」は削除。

**Why**: 3 つとも Patterson が公開している Equal Earth 壁地図の中央経線で、出典のない値を
混ぜない。0° は裂け目 180° がチュクチ半島とフィジーを切るが、10°E なら裂け目 170°W が
ベーリング海峡をほぼ陸を避けて通る。「地図屋がなぜ 10°E を選ぶか」がそのまま教材になる。
0° 自体はスライダーで合わせればすぐ見られる (user 判断、2026-09-05)。

## 図法は正積だけに絞る (Robinson を外す)

**判断**: Equal Earth / Mollweide / Eckert IV / Gall-Peters の 4 つ。非正積の Robinson は削除。

**Why**: 主題は「裂け目がどこに来るか」で、面積の歪みは別の教材の話。Robinson は
非正積の引き立て役として入れていたが、折衷図法ゆえ歪みが穏やかで比較用としても弱い。
全部正積にするとティソー円の説明が「どの図法でもどの円も面積が等しい」と一本になり、
UI の「正積 / 非正積」表示も不要になる (user 判断、2026-09-05)。

**将来**: 面積の歪みを見せたくなったら、引き立て役は Robinson でなく Mercator
(±85° で切る、外郭は長方形で経度回転に対して不変なので fit キャッシュ設計と両立)。

## 英語 UI は別ページでなく同一ファイル内の切替

**判断**: `i18n.ts` に ja / en 辞書を持ち、`data-i18n` 属性で流し込む。初期言語は
`navigator.language` (ja* なら日本語、それ以外は英語)、`?lang=` と見出し横のボタンで上書き。

**Why**: 別 HTML + リダイレクトは単一ファイル配布を壊し、`file://` では判定も遷移も効かない。
文字列は 30 個程度で +2 KB なので同梱が安い。URL には自動判定と違う時だけ `lang` を書く:
共有された URL を開いた人は自分の言語で見られ、明示的に切り替えた状態だけが共有される。

**書き出しへの影響**: SVG / PNG に文字は無いので言語非依存。

## 日本語 UI ではアルファベットを避ける

**判断**: 図法名 (イコールアース / モルワイデ / エケルト第4 / ゴール・ピーターズ)、タイトル、
プリセットの経度 (西経90° 等) を和文表記にする。例外は SVG / PNG (ファイル形式名) と
脚注の出典 (固有名・書誌)。図法名は `label` を言語別に持つ。

**Why**: 日本語話者向けの教材で英字が混じると読みの引っかかりになる (user 指摘、2026-09-05)。
